<?xml version="1.0" encoding="utf-8"?>
<!-- Medium-gray card background for voice result cards. Lighter than the old near-black
     (#E6262626) so the card stands out over the dimmed voice backdrop. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#F0555555" />
    <corners android:radius="16dp" />
</shape>
