<?xml version="1.0" encoding="utf-8"?>
<!-- Section header with uppercase text - aligned to section margins -->
<TextView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/tvHeader"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="@dimen/settings_section_margin_horizontal"
    android:layout_marginEnd="@dimen/settings_section_margin_horizontal"
    android:paddingStart="@dimen/settings_item_padding_horizontal"
    android:paddingEnd="@dimen/settings_item_padding_horizontal"
    android:paddingTop="@dimen/settings_header_padding_top"
    android:paddingBottom="@dimen/settings_header_padding_bottom"
    android:background="@android:color/transparent"
    android:textColor="@color/settings_text_header"
    android:textSize="@dimen/settings_header_text_size"
    android:textAllCaps="true"
    android:letterSpacing="0.05"
    tools:text="GENERAL" />
